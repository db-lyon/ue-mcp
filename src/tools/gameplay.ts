import { categoryTool, type ToolDef } from "../types.js";
import { actions as epicActions, schema as epicSchema } from "./epic/gameplay.generated.js";
import { specBp, schema as specSchema } from "./specs/gameplay.generated.js";
import { specBp as physicsSpecBp, schema as physicsSpecSchema } from "./specs/physics.generated.js";
import { specBp as massSpecBp, schema as massSpecSchema } from "./specs/mass.generated.js";

export const gameplayTool: ToolDef = categoryTool(
  "gameplay",
  "Gameplay systems: physics, collision, navigation, input, behavior trees, AI (EQS, perception, State Trees, Smart Objects), game framework.",
  {
    set_collision_profile:  physicsSpecBp("mutate", "Set the collision preset on every primitive component of a placed actor. Reports existed=true and unchanged=true when every component already used that profile. Rolls back through this same action with the profile that was there, addressed by actorPath, and marked lossy when more than one component was written since the record carries the FIRST one's profile. No record at all when the previous profile read as the 'Custom' sentinel, which names no profile the engine can look up: the response says so rather than emitting an inverse that would restore nothing.", "set_collision_profile"),
    set_simulate_physics:   physicsSpecBp("mutate", "Toggle physics simulation on every primitive component of a placed actor. Reports existed=true and unchanged=true when every component was already in that state. Rolls back through this same action with the previous flag, addressed by actorPath, and marked lossy when more than one component was written since the record carries the FIRST one's flag.", "set_simulate_physics"),
    add_impulse:            physicsSpecBp("mutate", "Apply an impulse (or force with mode='force') to a (PIE) actor's simulating physics body so its motion can be observed over time - loop level(read_actor_motion) to sample the response. No inverse: the solver integrates the impulse immediately, so an opposite impulse cancels the velocity but not the motion, and the response says rollbackPossible=false with that reason. unchanged is MEASURED, from the body's linear and angular velocity read either side of the push, so mode='force' reports unchanged (a force is integrated over the next substep, not applied instantly) and so does a vector the solver absorbed. It does NOT mean a replay is safe: two calls are two impulses, which is why there is no already* flag (#676).", "add_impulse"),
    set_collision_enabled:  physicsSpecBp("mutate", "Set the collision mode on every primitive component of a placed actor: NoCollision | QueryOnly | PhysicsOnly | QueryAndPhysics. Reports existed=true and unchanged=true when every component already held that mode. Rolls back through this same action with the previous mode, addressed by actorPath; marked lossy when more than one component was written, because the record carries the mode read from the FIRST of them and components that differed all come back with that one's. gameplay(set_collision) with a componentName is the per-component form.", "set_collision_enabled"),
    set_collision:          physicsSpecBp("mutate", "Unified collision authoring for a placed actor (actorLabel or actorPath) or a Blueprint component template (assetPath+componentName). Apply any of: collisionProfile, collisionEnabled (NoCollision|QueryOnly|PhysicsOnly|QueryAndPhysics), objectType (channel name), responseToAllChannels (Block|Overlap|Ignore), responses ({channel: Block|Overlap|Ignore}). Profile is applied first, then overrides. componentName optional for actors (defaults to all primitive components), required for Blueprint templates. Reports unchanged=true when the component already held everything the call wrote, compared against a full before/after read. The inverse is this same action with the previous values: writing a profile or responseToAllChannels resets the whole response table, so both make the record carry every channel, not only the ones named. A component whose profile read as the 'Custom' sentinel comes back through its collision mode, object type and full response table instead, since that name resolves to no profile; that and a multi-component target are the two cases marked lossy (#545).", "set_collision"),
    set_physics_properties: physicsSpecBp("mutate", "Set mass/damping/gravity on every primitive component body of a placed actor. Only the fields passed are written, and only those are in the inverse, which is this same action carrying the values that were there. A call that passed none of them reports existed=true and unchanged=true rather than a bare success. Marked lossy when more than one component body was written, because the record carries the values read from the FIRST of them.", "set_physics_properties"),
    rebuild_navigation:     specBp("mutate", "Rebuild navmesh. No inverse: the navmesh is derived data recomputed from the level, so nothing restores the previous one and the response says rollbackPossible=false.", "rebuild_navigation"),
    find_nav_path:          specBp("read", "Synchronous nav-path query between two world points. Returns valid/partial/length plus the polyline. The standard 'why doesn't my AI move?' diagnostic. pathfindingContext (actor label) or pathfindingContextPath (object path) picks the agent and filter the query uses (#424/#983).", "find_nav_path"),
    list_nav_invokers:      specBp("read", "Enumerate actors carrying a NavigationInvokerComponent + their tile generation/removal radii. Diagnoses 'no navmesh in this region' caused by missing or mis-sized invokers (#424).", "list_nav_invokers"),
    get_navmesh_info:       specBp("read", "Query nav system.", "get_navmesh_info"),
    project_to_nav:         specBp("read", "Project point to navmesh.", "project_point_to_navigation"),
    spawn_nav_modifier:     specBp("mutate", "Place a NavModifierVolume. The brush is built for real (a bare spawn leaves an AVolume with no geometry, which the engine skips entirely - it requires both Brush and AreaClass). extent is a HALF-size in world units, default 100. areaClass defaults to NavArea_Null, which cuts a hole in the navmesh; pass NavArea_Obstacle to make the region costly instead, or any UNavArea subclass.", "spawn_nav_modifier_volume"),
    create_input_action:    specBp("mutate", "Create InputAction.", "create_input_action"),
    create_input_mapping:   specBp("mutate", "Create InputMappingContext.", "create_input_mapping_context"),
    list_input_assets:      specBp("read", "List input assets. Every InputAction and InputMappingContext is one row under `assets`, tagged with its kind and sorted by object path; inputActions and inputMappingContexts hold this page's rows of each kind, and inputActionCount / inputMappingContextCount count the whole listing.", "list_input_assets"),
    read_imc:               specBp("read", "Read InputMappingContext mappings. Returns {key, inputAction, triggers[], modifiers[]}.", "read_imc"),
    get_applied_imcs:       specBp("read", "Read applied Input Mapping Contexts (name, path, priority, registrationCount), highest priority first - the order Enhanced Input resolves in. Covers EVERY running PIE world by default, so a multiplayer client can be inspected, not just the primary/server world; narrow with pieInstance and/or playerIndex. Pass mappingContext for a direct hasRequestedContext yes/no. A remote controller with no LocalPlayer says so rather than reporting an empty list. BREAKING in 1.1.36: results are now nested under worlds[].players[] (was a flat appliedContexts[] for one world), the array is mappingContexts (was appliedContexts), each entry uses path (was imc), and a call with PIE stopped returns success with worldCount 0 instead of erroring (#604/#778).", "get_input_mapping_contexts"),
    list_input_mappings:    specBp("read", "Alias for read_imc. List key→action bindings with triggers/modifiers.", "list_imc_mappings"),
    add_imc_mapping:        specBp("mutate", "Add key mapping to IMC. Idempotent: the same (action, key) pair reports existed. Rolls back through remove_imc_mapping on that pair, with nothing lost - the mapping is created bare. Returns saved, persisted, packageDirty, packageName, and persistError when not persisted.", "add_imc_mapping"),
    set_mapping_modifiers:  specBp("mutate", "Set modifiers/triggers on an IMC mapping. Each modifier OR trigger is either {type:'<ShortName>', <prop>:<val>} or {class:'/Script/Module.Class', properties:{...}} - the class form resolves any custom UInputModifier/UInputTrigger subclass (e.g. type:'Hold' | class:'/Script/EnhancedInput.InputTriggerHold', with HoldTimeThreshold). Unresolvable trigger specs are reported in failedTriggers and never leave a null entry (#649/#725). Reports unchanged=true when the rebuilt lists hold the same classes carrying the same property values, compared as an export of every object's every property either side of the write - so retuning a Hold threshold on the same trigger class reads as an update rather than as a no-op. The inverse replays this call with the classes that were on the mapping, each rebuilt at its CLASS DEFAULTS, so it is marked lossy whenever the previous objects had tuned properties. Returns saved, persisted, packageDirty, packageName, and persistError when not persisted.", "set_mapping_modifiers"),
    remove_imc_mapping:     specBp("mutate", "Remove an IMC mapping. Idempotent when selected by (inputActionPath + key): a pair that is not there reports alreadyDeleted=true, so a replayed rollback is safe. Rolls back through add_imc_mapping on the same pair, marked lossy because the mapping is re-added at the END of the list and its modifiers and triggers are not carried. Select the mapping by mappingIndex, or by inputActionPath plus key; save defaults to true. Returns saved, persisted, packageDirty, packageName, and persistError when not persisted (#158).", "remove_imc_mapping"),
    set_imc_mapping_key:    specBp("mutate", "Rebind an IMC mapping to a new key. Reports unchanged=true when the mapping already used that key, and otherwise rolls back to the previous key by mappingIndex, which this call does not move. Select the mapping by mappingIndex, key or inputActionPath. Returns saved, persisted, packageDirty, packageName, and persistError when not persisted (#158).", "set_imc_mapping_key"),
    set_imc_mapping_action: specBp("mutate", "Retarget an IMC mapping to a different InputAction. Reports unchanged=true when the mapping already used that action, and otherwise rolls back to the previous action by mappingIndex. A mapping that had NO action before cannot be rolled back, because the inverse needs a loadable path; the response says so. Select the mapping by mappingIndex, key or inputActionPath. Returns saved, persisted, packageDirty, packageName, and persistError when not persisted (#158).", "set_imc_mapping_action"),
    read_input_action:      specBp("read", "Read an InputAction back: valueType, actionDescription, the four consumption/pause flags, accumulationBehavior, playerMappableKeySettings, and the ACTION's own instanced triggers[] and modifiers[] with each one's class, current property values and objectPath. Nothing could read an InputAction before, so an authored one could not be verified. There are no typed setters for any scalar here: they are plain UPROPERTYs, so write them with editor(set_property) at the returned objectPath, and add or remove trigger/modifier entries with set_action_triggers. These arrays are the action's own, applied after every mapping's; read_imc reports the per-mapping ones. Returns inputActionPath, name, objectPath, valueType, triggers[], modifiers[], triggerCount, modifierCount.", "read_input_action"),
    set_action_triggers:    specBp("mutate", "Replace the ACTION's own Triggers and/or Modifiers arrays on an InputAction. asset(set_property) cannot do this: both are Instanced TArrays and the JSON property setter only assigns an object it can load from a path, so it can neither mint nor destroy the subobjects they hold. Each entry is {type:'Hold', HoldTimeThreshold:0.5} or {class:'/Script/EnhancedInput.InputTriggerHold', properties:{...}}, the same grammar set_mapping_modifiers takes, except an unresolvable class or an unknown property is an ERROR here rather than a silent drop, and both arrays are fully built before either is assigned. A supplied array replaces that array wholesale; an omitted one is left alone unless clear is true, which empties both. Repeating the same call reports unchanged=true. Rollback restores the exact prior arrays. Returns triggerCount, modifierCount, triggersReplaced, modifiersReplaced, unchanged.", "set_action_triggers"),
    set_player_mappable_settings: specBp("mutate", "Create or update the protected Instanced UPlayerMappableKeySettings subobject on an InputAction. asset(set_property) cannot mint that subobject, and the pointer is protected, so this is the authoring path. mappingName is the stable FName saved with the mapping and must be non-empty. displayName and displayCategory are optional display text; an omitted one is left alone. Repeating the same write reports unchanged=true. save defaults to true. Returns the authored metadata plus saved, persisted, packageDirty and persistError when not persisted.", "set_player_mappable_settings"),
    apply_mapping_context:  specBp("mutate", "Apply an InputMappingContext to a LIVE PIE player at a priority, the write half get_applied_imcs never had. Calls the local player's Enhanced Input subsystem, which is an engine call rather than a property write, and forces the control-mapping rebuild immediately so the very next get_applied_imcs sees it. Idempotent: already applied at the same priority reports alreadyApplied=true and changes nothing; applied at a different priority reports previousPriority and rolls back to it. This is runtime state, so it affects the live player only and is gone when PIE stops. Returns mappingContext, priority, alreadyApplied, pieInstance, playerIndex, worldPath, scope.", "apply_mapping_context"),
    remove_mapping_context: specBp("mutate", "Remove an InputMappingContext from a LIVE PIE player. The inverse of apply_mapping_context and its rollback target. Idempotent: a context that is not applied reports alreadyAbsent=true rather than erroring, and rollback re-applies it at the priority it was removed from. Runtime state only, gone when PIE stops. Returns mappingContext, alreadyAbsent, removedPriority, pieInstance, playerIndex, worldPath.", "remove_mapping_context"),
    get_action_value:       specBp("read", "Read what an InputAction is worth on a LIVE PIE player right now: value {x,y,z}, magnitude, nonZero, triggerEvent, elapsedProcessedTime, elapsedTriggeredTime, lastTriggeredWorldTime and the keys currently mapped to it. This is the only way to prove input arrived, because FInputActionInstance is transient per-player state and its accessors are plain C++ methods, not UFUNCTIONs, so editor(invoke_function) cannot reach them. Omit inputActionPath for every action the player currently has bound. An action with hasInstanceData=false is bound by no applied context, which is a different failure from bound-but-reading-zero. The engine zeroes value outside a Triggered event, so read triggerEvent before concluding nothing arrived. Returns actions[], actionCount, pieInstance, playerIndex, worldPath.", "get_action_value"),
    validate_input:         specBp("read", "Audit Enhanced Input assets for the failures that produce no error anywhere: a mapping with no InputAction, a key name that is not a registered FKey, a duplicate action plus key pair inside one context, a null entry in a Triggers or Modifiers array (which fails asset validation on save), an empty mapping context, an InputAction no context maps, and the quiet one, an Axis2D/Axis3D action driven by a 1D or boolean key with no Swizzle Input Axis Values modifier on the mapping or the action, which reads zero on every component past X forever. Pass imcPath for one context, or sweep every InputMappingContext under directory. Read-only. Returns scanned[], contextCount, mappingsChecked, unmappedActions[], problems[] ({severity, code, message, asset, mappingIndex}), errorCount, warningCount, valid.", "validate_input"),
    list_behavior_trees:    specBp("read", "List behavior trees, sorted by object path.", "list_behavior_trees"),
    get_behavior_tree_info: specBp("read", "Inspect a BehaviorTree asset: root node, blackboard asset, every blackboard key (name, type, instanceSynced, description) plus the keys inherited from the blackboard's parent chain, and the node/decorator counts (#887).", "get_behavior_tree_info"),
    read_behavior_tree_graph: specBp("read", "Walk a BT asset: composites, tasks, decorators, services, each with a stable nodePath ('Root.Children[0].Decorators[1]') you pass to read_bt_node_properties / set_bt_node_property. Every decorator reports its full runtime config - blackboardKey {selectedKeyName, selectedKeyType}, basicOperation (Set|NotSet), arithmeticOperation, textOperation, flowAbortMode, notifyObserver, inverseCondition, int/float/stringValue and staticDescription - which is what answers 'the tree is running but the wrong branch fires'. Set includeProperties for each node's own UPROPERTY values, propertyNames to narrow those, includeInherited to keep UBTNode's own fields (#888).", "read_behavior_tree_graph"),
    read_bt_node_properties: specBp("read", "Filtered read of BT node UPROPERTY values: pick nodes by nodeClass, nodeName, nodePath or kind and get back only their own properties, instead of an unfiltered whole-asset dump. Narrow further with propertyNames. A UE 5.8 FValueOrBBKey_* field (AcceptableRadius, WaitTime, FilterClass) is unpacked into {defaultValue, key, isBound, baseClass, text}; class defaults use the canonical class object path while text preserves Unreal's export form (#919).", "read_bt_node_properties"),
    list_bt_tasks: specBp("read", "Inventory BTTask nodes with their name, class, parent path and FilterClass. Pass assetPath for one tree, or directory (+recursive) to sweep many. On UE 5.8 BTTask_MoveTo.FilterClass is a FValueOrBBKey_Class, so it is reported as {defaultValue, defaultValueName, key, isBound, baseClass, text} - the shape get_editor_property reads as empty. filterClassOnly keeps only tasks that declare one (#940).", "list_bt_tasks"),
    set_bt_node_property: specBp("mutate", "Scoped write onto one owned BT node subobject. Pick the node with nodePath (from read_behavior_tree_graph), nodeName, nodeClass or kind - the write refuses to run unless exactly one node matches - then set property + value, or a properties map of dotted/indexed paths ('Lines[1].Text', 'BlackboardKey.SelectedKeyName'). A scalar aimed at a UE 5.8 FValueOrBBKey_* field lands on its DefaultValue; an object writes the struct's own fields, so {Key: 'MyKey'} binds it to a blackboard key. Every target is read back and reported with its previous value (#919).", "set_bt_node_property"),
    list_bt_graph_nodes: specBp("read", "List the EDITOR-GRAPH nodes of a BehaviorTree, which is the layer add_bt_node / move_bt_node / remove_bt_node address. Each entry carries its guid (the primary key: stable across recompiles and unaffected by inserting a sibling), category (root|composite|task|decorator|service), class, parentGuid, indexInParent, childGuids in execution order, decoratorGuids, serviceGuids, graph position and any node error. runtimePath cross-references the same node's read address ('Root.Children[0]') so one listing serves both surfaces. A tree that has never been opened in the BT editor reports hasGraph false and no nodes; add_bt_node seeds the graph (#889).", "list_bt_graph_nodes"),
    add_bt_node: specBp("mutate", "Add a composite, task, decorator or service to a BehaviorTree's editor graph and recompile it into the runnable tree. This is the authoring half that reflection cannot reach: UBehaviorTreeGraph::UpdateAsset, which turns the graph into the UBTCompositeNode tree, is editor-only and unexposed, so nodes spawned by script alone never become a valid tree. parent takes a guid from list_bt_graph_nodes, a runtime address from read_behavior_tree_graph, a unique node name, or 'root' (the default). Decorators and services attach as subnodes of their parent rather than as children. index places the node among its siblings, which is the execution order a Selector or Sequence is defined by; on a SimpleParallel it picks the output (0 = main task, 1 = background). properties writes UPROPERTY values (a bare number aimed at a UE 5.8 FValueOrBBKey_* field lands on its DefaultValue), and blackboardKeys points FBlackboardKeySelector fields at named blackboard keys, resolved against the tree's blackboard (#889).", "add_bt_node"),
    move_bt_node: specBp("mutate", "Reconnect a BehaviorTree node to a different parent, reorder it among its siblings, or both, then recompile. Pass parent to reparent and index to reorder; at least one is required. Child order is what a Selector or Sequence executes by, so this is behaviour rather than layout. Decorators and services move between their owners' subnode lists. Moving a node under itself or its own descendant is refused, and the whole subtree travels with it. Returns the parent's siblingOrder after the compile (#947).", "move_bt_node"),
    remove_bt_node: specBp("mutate", "Delete a node from a BehaviorTree's editor graph and recompile. A composite takes its whole branch with it, so the compiled tree never reports nodes that no longer run. Decorators and services detach from their owner. The root node cannot be removed. Returns every removed node's guid and class (#889).", "remove_bt_node"),
    set_bt_task_property: specBp("mutate", "Write a property on one BT task node, FilterClass included. Same selector and dotted-path rules as set_bt_node_property. Retarget a MoveTo filter with property='FilterClass' and a class path, a short class name or a Blueprint asset path; pass null to clear it, or {Key: 'MyKey'} to bind it to a blackboard key. The new value is read back and returned (#940).", "set_bt_task_property"),
    create_blackboard:      specBp("mutate", "Create Blackboard.", "create_blackboard"),
    add_blackboard_key:     specBp("mutate", "Add a typed key to a Blackboard asset. baseClass types an Object/Class key - Behaviour Tree nodes filter on it, so an untyped key silently will not bind. For keyType=Enum pass the enum via enumType (or baseClass). Idempotent: a key of that name already on the Blackboard reports existed=true and emits no record, so a replay cannot delete a key it did not create. A key this call DID declare rolls back exactly, through remove_blackboard_key on the same name, and that action is itself idempotent on a name it cannot find (#250).", "add_blackboard_key"),
    remove_blackboard_key:  specBp("mutate", "Remove a key from a Blackboard asset by name. Idempotent. Rolls back through add_blackboard_key with the key type it carried, including the baseClass of an Object/Class key and the enum of an Enum key. Always marked lossy: that action takes a name and a type and nothing else, so bInstanceSynced and the editor-only description and category are not restored, and a key that was not the last one comes back appended with a new key ID (Behaviour Tree nodes bind by name, so they still resolve). A key whose type is not one of the ten add_blackboard_key accepts - a NativeEnum key, or any project-defined UBlackboardKeyType subclass - gets no record at all rather than one that would be refused on replay (#469).", "remove_blackboard_key"),
    set_blackboard_parent:  specBp("mutate", "Set Parent on a BlackboardData asset (canonical UE child-of-parent pattern). Pass parentPath=\"None\" or omit to clear. autoPruneDuplicateKeys (default true) removes own-keys that the parent chain already defines so the BT compiler accepts the child (#469).", "set_blackboard_parent"),
    read_blackboard:        specBp("read", "Read a Blackboard asset: parent path, ownKeys, inheritedKeys (walks the parent chain) (#469).", "read_blackboard"),
    list_bt_node_classes:   specBp("read", "Enumerate every concrete BehaviorTree node class on this build (composites, tasks, decorators, services). Filter by kind to narrow; an unrecognised kind is refused rather than answered with an empty palette. Every class is one row under `classes`, tagged with its kind and sorted by class path within each kind; composites/tasks/decorators/services hold this page's rows of each kind, and compositeCount/taskCount/decoratorCount/serviceCount count the whole build. Useful for discovering plugin-supplied decorator/task classes without grepping engine + plugin source (#494).", "list_bt_node_classes"),
    set_behavior_tree_blackboard: specBp("mutate", "Rebind a BehaviorTree asset's BlackboardAsset reference. The C++ field is protected so reflection is the only writable path (#250).", "set_behavior_tree_blackboard"),
    create_behavior_tree:   specBp("mutate", "Create behavior tree. blackboardPath is assigned before the first save; an unknown path creates nothing.", "create_behavior_tree"),
    create_eqs_query:       specBp("mutate", "Create EQS query.", "create_eqs_query"),
    list_eqs_queries:       specBp("read", "List EQS queries.", "list_eqs_queries"),
    list_eqs_types:         specBp("read", "List every EQS generator, test and context class this build has, with the short name the editor's dropdown shows (which add_eqs_generator and add_eqs_test both accept) and the full class path. Every class is one row under `types`, tagged with its kind and sorted by class path within each kind; generators/tests/contexts hold this page's rows of each kind, and generatorCount/testCount/contextCount count the whole build. Call this before authoring: the class names are long and guessing one costs a round trip.", "list_eqs_types"),
    read_eqs_query:         specBp("read", "Read an EQS query's structure back: every option with its generator, every test with its index, class, purpose (Filter|Score|FilterAndScore), filter type, scoring equation, and the objectPath that editor(set_property) uses to configure it. Also reports problems[] and runnable: an option with no generator produces no items, and a query whose tests are all filter-only scores every survivor equally so its 'best' item is arbitrary. This is how you verify authoring without opening the editor.", "read_eqs_query"),
    add_eqs_generator:      specBp("mutate", "Add an option with a generator to an EQS query, which is what makes it produce items at all. generatorClass takes the short name ('ActorsOfClass', 'OnCircle', 'SimpleGrid') or a full class path; list_eqs_types lists them. Returns generatorObjectPath, which editor(set_property) uses to tune the generator (radius, extents, class filters). Two options with the same generator class are allowed, because two donuts at different radii is a real thing to want.", "add_eqs_generator"),
    add_eqs_test:           specBp("mutate", "Add a test to one option of an EQS query. testClass takes the short name ('Distance', 'Trace', 'Pathfinding', 'Dot') or a full path. purpose sets what the test is for: filter discards items, score ranks them, both does each. Returns testObjectPath for editor(set_property), which is how ScoringFactor, FilterType, FloatValueMin/Max, ScoringEquation and the test's own parameters are set.", "add_eqs_test"),
    remove_eqs_option:      specBp("mutate", "Remove a whole option, generator and all its tests, from an EQS query. Without this an option could be added and never taken away, which left add_eqs_generator with no inverse. Idempotent: an index past the end reports alreadyRemoved rather than failing. Emits a rollback that restores the generator class, and says plainly that generator tuning and the option's tests are NOT restored by it.", "remove_eqs_option"),
    remove_eqs_test:        specBp("mutate", "Remove one test from an option by index. Reports remainingTests and warns that later indices shifted down, which matters when removing several in one pass.", "remove_eqs_test"),
    reorder_eqs_tests:      specBp("mutate", "Reorder an option's tests. Test order matters in EQS: a cheap filter placed first discards items before an expensive test (a trace, a pathfind) ever runs on them. order is the current indices in the order you want them, and must be a full permutation, so a mistake is refused rather than silently dropping a test.", "reorder_eqs_tests"),
    list_ai_agents:         specBp("read", "List every actor in a live world carrying a BrainComponent, with its pawn and controller identity, running/paused state, current and root BehaviorTree, active node and blackboard asset. Call this FIRST: it is how you discover a target for the other BT runtime actions. A zero result reports how many actors were scanned and how many brains were found, because AI only exists in a game world.", "list_ai_agents"),
    get_bt_runtime:         specBp("read", "Read the LIVE execution path of a running BehaviorTree, which is what tells you whether an authored tree actually works. Returns the active node with its task status, the composite ancestor chain above it, current versus root tree (so a running subtree is visible), running/paused/restartPending/abortPending, the decorators and services currently active, and the engine's own DescribeActiveTasks dump. A tree must have been started first, by run_behavior_tree or the game's own BeginPlay; the first search runs on the next tick, so activeNode is null immediately after a start. Pass actorLabel or actorPath.", "get_bt_runtime"),
    get_live_blackboard:    specBp("read", "Dump every key on a running BlackboardComponent with its name, key id, type, instance-sync flag, the engine's own value description, and a typed value for Bool/Int/Float/String/Name/Vector/Rotator/Object/Class/Enum. Requires a blackboard to have been initialised, which happens when a tree with a BlackboardAsset starts. Pass key for one entry; a miss lists every real key name. Pass actorLabel or actorPath.", "get_live_blackboard"),
    set_live_blackboard:    specBp("mutate", "Write one key on a running blackboard through the typed SetValueAs accessor, so the key-change notification fires and observing decorators re-evaluate their branch. A raw property write would NOT notify, which is why this exists. Reports the previous and current value, changed, and alreadySet when the key already held it, and emits a rollback that writes the captured previous value back. Pass actorLabel or actorPath.", "set_live_blackboard"),
    run_behavior_tree:      specBp("mutate", "Start a BehaviorTree on a live agent via AAIController::RunBehaviorTree, which also creates the BehaviorTree component and initialises the blackboard. Idempotent: the same tree already running returns alreadyRunning without restarting unless restartIfRunning is set. Rollback stops the tree, or restarts whatever was running before, marked lossy because a restart cannot restore execution position. Pass actorLabel or actorPath.", "run_behavior_tree"),
    stop_behavior_tree:     specBp("mutate", "Stop, restart, pause or resume a running BehaviorTree. Safe to call twice: a stop on a stopped tree returns alreadyStopped and a pause on a paused one alreadyPaused. Pause and resume roll back to each other; stop rolls back to a run of the tree that was running, marked lossy because the instance stack it destroyed cannot be re-entered. Pass actorLabel or actorPath.", "stop_behavior_tree"),
    read_perception:        specBp("read", "Read an AIPerceptionComponent, from a Blueprint's construction script or a live actor, and return every sense config with its class, maxAge, startsEnabled, affiliation flags, full parameter dump and objectPath. Tune any sense with editor(set_property) at that objectPath; there are deliberately no per-parameter setters. A problems[] array names the faults a setup cannot report about itself: no sense configs at all, every sense starting disabled, LoseSightRadius below SightRadius, all affiliation flags false, and no sight stimuli source anywhere in the world. Pass blueprintPath, or actorLabel or actorPath.", "read_perception"),
    remove_sense:           specBp("mutate", "Delete one sense config from an AIPerceptionComponent template's Instanced SensesConfig array. set_property cannot do this because it cannot destroy instanced subobjects. Idempotent: an index past the end, or a sense that is not configured, returns alreadyRemoved rather than failing, so a rollback replays safely. Emits a configure_sense rollback carrying every value that differed from the class defaults, and reports rollbackIsLossy plus rollbackUncapturedProperties when a tuned value could not be represented. Pass index or senseType.", "remove_sense"),
    get_perceived_actors:   specBp("read", "List what an AI currently perceives, what it still remembers, and which of those are hostile, each with distance, stimulus age and last-known location. This reads PerceptualData, a bare C++ TMap that is not a UPROPERTY, so nothing else on this surface can reach it. Finds the component on the actor or on the AIController possessing it. An empty result explains the usual causes rather than returning a bare zero. Pass actorLabel or actorPath.", "get_perceived_actors"),
    check_perception:       specBp("read", "Answer whether one actor perceives another right now, with the youngest stimulus age, the last known location, and how far that has drifted from the target's real position. Breaks the answer down per configured sense, so you can see which sense is carrying the detection and which is not firing. Uses HasAnyActiveStimulus and GetYoungestStimulusAge, which are not UFUNCTIONs and so cannot be reached through editor(invoke_function). Pass perceiverLabel or perceiverPath, and targetLabel or targetPath.", "check_perception"),
    report_noise_event:     specBp("mutate", "Inject a hearing or damage stimulus into a live world so a perception setup can be exercised without a real game event. Reports which perception components in the world have that sense configured, with each listener's distance and, for hearing, whether the noise falls inside its HearingRange, so an event nobody could receive is distinguishable from a silent one. The stimulus is delivered on the AI system's next update, so read the outcome with check_perception or get_perceived_actors.", "report_noise_event"),
    run_eqs_query:          specBp("read", "Run an EQS query against a live world and return the scored items, which is the only way to know an authored query does what you meant. Returns each item's score and location, plus actorLabel/actorPath for actor items, sorted by the engine's own ranking. Needs a world with an AI system, so PIE rather than the pure editor world. querierLabel names the actor the query runs FROM, which every querier-relative context resolves against; without one those contexts silently produce nothing. An empty result is not an error and explains itself: runMode 'all' returns only items that pass every filter test, so a filter left at its default range rejects nearly everything, while 'best' ranks instead and still returns one.", "run_eqs_query"),
    add_perception:         specBp("mutate", "Add an AIPerceptionComponent to a Blueprint and configure its senses. senses takes short names (Sight, Hearing, Damage, Touch, Team, Prediction), AISenseConfig_* names, or class paths - a component with no sense configs perceives nothing.", "add_perception_component"),
    configure_sense:        specBp("mutate", "Add OR tune an AI perception sense config on the blueprint's AIPerceptionComponent. settings are applied whether the sense was just created or was already there, so 'add a sense then tune it' writes on the second call. Reports the three outcomes apart: created, existed+updated (changedProperties), existed+unchanged (unchangedProperties, asset untouched). An unknown settings key is refused with the valid property names and nothing is written; a value that will not convert restores what it had already applied.", "configure_ai_perception_sense"),
    get_state_tree_runtime: specBp("read", "Read a running StateTreeComponent's active state names in PIE (the 'brain' state) (#654).", "get_state_tree_runtime"),
    create_state_tree:      specBp("mutate", "Create a StateTree with editor data, a schema and a root state, so states/tasks are authorable via statetree(*) and persist across save/load (#653). The schema is required for the tree to compile at all: omit it to take StateTreeComponentSchema, then StateTreeAIComponentSchema, then whatever concrete schema this editor has, and read schemaSource / schemaNote to see which was used. Refuses (and writes nothing) when a named schema does not resolve, or when the editor has no schema class at all, listing availableSchemas and naming the plugin to enable (#833).", "create_state_tree"),
    list_state_trees:       specBp("read", "List StateTrees, sorted by object path.", "list_state_trees"),
    add_state_tree_component: specBp("mutate", "Add StateTreeComponent.", "add_state_tree_component"),
    create_smart_object_def: specBp("mutate", "Create SmartObjectDefinition. Pass defaultBehaviorClass to give it a default behavior definition, which is what makes every slot added later legal: the editor rejects a definition whose slot has no behavior and whose definition has no default. Returns definitionValid + defaultBehaviorCount (#833).", "create_smart_object_definition"),
    add_smart_object_component: specBp("mutate", "Add SmartObjectComponent.", "add_smart_object_component"),
    add_smart_object_slot: specBp("mutate", "Append a FSmartObjectSlotDefinition to a SmartObjectDefinition's Slots array. Pass behaviorClass to give the slot its behavior definition in the same call; without one the definition needs a default or the editor's asset check rejects the asset, which the response reports as definitionValid=false plus the exact fix. Returns the new slotIndex. Rollback removes the slot (#416/#833).", "add_smart_object_slot"),
    set_smart_object_slot: specBp("mutate", "Mutate an existing slot's offset/rotation/tags/name. Omitted fields keep their current values (#416). Reports unchanged=true when the slot ends up identical, compared as exported struct text before and after. The inverse writes back exactly the fields this call overwrote, captured through the same type-checked accessors the writer uses, so a field supplied in the wrong JSON shape is neither written nor carried.", "set_smart_object_slot"),
    remove_smart_object_slot: specBp("mutate", "Remove a slot by index. Idempotent: out-of-range returns alreadyDeleted=true. Removing the LAST slot rolls back through add_smart_object_slot, which appends it to the same index (lossy on the slot BehaviorDefinitions, which that action can only restore one class of, with no instance values). Removing any other slot has no inverse, because re-adding would append it to the end and leave every later slotIndex shifted; the full removed slot is reported as removedSlot for manual recovery (#416).", "remove_smart_object_slot"),
    list_smart_object_slots: specBp("read", "List slots on a SmartObjectDefinition with index, offset, rotation, and raw text (#416).", "list_smart_object_slots"),
    add_smart_object_slot_behavior: specBp("mutate", "Attach a behavior definition (UBehaviorDefinition asset or class) to a slot's BehaviorDefinitions array. Pass instanceProperties to seed UPROPERTYs on a freshly-spawned class-instance. No inverse: nothing removes an entry from a slot BehaviorDefinitions array, and removing the slot would undo more than this did, so the response says rollbackPossible=false and names the behaviorIndex it added (#416).", "add_smart_object_slot_behavior"),
    add_smart_object_default_behavior: specBp("mutate", "Add a behavior definition to the SmartObjectDefinition's DefaultBehaviorDefinitions, the list every slot falls back to when it provides none of its own. This is the definition-wide half of add_smart_object_slot_behavior, and it is what makes an existing definition that already carries bare slots pass the editor's asset check. Returns definitionValid and, when slots are still short, the indices that are (#833).", "add_smart_object_default_behavior"),
    create_game_mode:       specBp("mutate", "Create GameMode BP. parentClass must derive from GameModeBase (short name, /Script path, or a Blueprint asset path).", "create_game_mode"),
    create_game_state:      specBp("mutate", "Create GameState BP. parentClass must derive from GameStateBase.", "create_game_state"),
    create_player_controller: specBp("mutate", "Create PlayerController BP. parentClass must derive from PlayerController.", "create_player_controller"),
    create_player_state:    specBp("mutate", "Create PlayerState BP. parentClass must derive from PlayerState.", "create_player_state"),
    create_hud:             specBp("mutate", "Create HUD BP. parentClass must derive from HUD.", "create_hud"),
    set_world_game_mode:    specBp("mutate", "Set level GameMode override.", "set_world_game_mode"),
    get_framework_info:     specBp("read", "Get level framework classes.", "get_game_framework_info"),
    ensure_mass_entity_config: massSpecBp("mutate", "Idempotently create or update a MassEntityConfigAsset and author its ordered trait list. Needs the MassGameplay/MassSpawner plugins enabled, otherwise the call fails saying so. Each traits[] entry is {class (a concrete UMassEntityTraitBase subclass, or traitClass), properties? (fields written onto the trait instance, dotted paths allowed)}; the order is the asset's order and duplicate classes are refused. Every property is validated against a transient copy of the trait first, so a bad property late in the list leaves nothing half-authored, and the whole write runs inside one transaction. An existing asset may only be extended: fewer traits than it has, or a different class at an existing index, is refused as destructive rather than applied. onConflict is skip|error|update (default update). Returns assetPath, traits (count), propertiesSet, and created/existed/updated.", "ensure_mass_entity_config"),
    read_mass_entity_config: massSpecBp("read", "Read back a MassEntityConfigAsset authored by ensure_mass_entity_config: its ordered traits and every non-transient property on each one, exported as text (object and soft-object properties come back as asset paths). Needs the MassGameplay/MassSpawner plugins enabled. Returns assetPath, assetClass, traitClasses[] (class paths in order), traits[] ({index, classPath, className, properties}) and traitCount.", "read_mass_entity_config"),
    list_mass_types:        specBp("read", "List the concrete UMassEntityTraitBase and UMassProcessor subclasses this editor has loaded, which is what ensure_mass_entity_config's traits[].class accepts and there was no way to discover. Each entry carries className, classPath, module, parentClass and the CDO objectPath, plus validTargetConfig for a trait and processingPhase/executionFlags/autoRegister/executionOrder for a processor. Those are all config UPROPERTYs on the default object, so tune them with editor(set_property) at objectPath; no typed setters are shipped. Enumerates LOADED classes only, so a Mass module nothing has referenced yet contributes nothing. Needs the MassGameplay and MassEntity plugins. Returns traits[], processors[], traitCount, processorCount, unavailable[], note.", "list_mass_types"),
    remove_mass_trait:      specBp("mutate", "Remove one trait from a MassEntityConfigAsset's Config.Traits, the half of trait CRUD nothing could do. ensure_mass_entity_config refuses to shrink a config on purpose, and asset(set_property) cannot help because Config.Traits is an Instanced TArray whose subobjects a property write can neither mint nor destroy. Select with traitClass (idempotent: removes the first trait of that class, and a second call reports alreadyRemoved=true) or index (positional: removes whatever currently sits there). Removing the LAST trait rolls back exactly, through ensure_mass_entity_config with the captured prior list; removing an earlier one cannot, because ensure_mass_entity_config only appends, so the result says so and returns priorTraits plus the two-call recipe instead of a rollback that would fail. Returns assetPath, removedIndex, removedClass, traitCount, alreadyRemoved, rollbackAvailable.", "remove_mass_trait"),
    reorder_mass_traits:    specBp("mutate", "Set the order of a MassEntityConfigAsset's Config.Traits. Trait order is the order the entity template is built in, and nothing else could change it: ensure_mass_entity_config refuses an existing class that differs from the requested one at the same index. order is the current trait indices in the order wanted and must be a full permutation, validated in whole before anything moves. An identity permutation reports unchanged=true. Rollback is the inverse permutation, so this is an exact undo. Returns assetPath, traitCount, traitClasses[], unchanged.", "reorder_mass_traits"),
    validate_mass_entity_config: specBp("read", "Audit a MassEntityConfigAsset and its whole Config.Parent chain: parent cycles, a null or wrong-typed trait entry, an abstract/deprecated/reinstanced trait class, a trait class provided twice (traits are combined uniquely, so the second copy is dead), an unset object or soft-object reference on a trait (the usual reason a Mass agent spawns and renders nothing) and an entirely empty config. Read-only, and it does NOT run the engine's own FMassEntityConfig ValidateEntityTemplate, which is a C++-only API that would need MassSpawner linked. Every reported trait carries its objectPath for editor(set_property). Returns assetPath, parentChain[], traits[], traitCount, problems[] ({severity, code, message, config, traitIndex, objectPath}), errorCount, warningCount, valid.", "validate_mass_entity_config"),
    query_zone_graph:       specBp("read", "Query the BUILT Zone Graph in a world. The graph lives in AZoneGraphData.ZoneStorage as parallel index arrays, where a lane names a half-open range into LanePoints and another into LaneLinks, so a raw editor(get_property) dump of it is technically reachable and unusable; this resolves the indirection. queryMode is summary (lane/zone counts and tagsInUse), lanes (index, zoneIndex, width, length, tags, start and end point), lane (one lane's full polyline plus its linked lanes, needs laneIndex) or nearest (closest point on any lane to location, within radius). Filter by tag NAME with tags[]. Zone SHAPE authoring is not here and does not need to be: spawn AZoneShape with level(spawn_actor), write its Points with editor(set_property), and call SetShapeType/SetTags/SetPolygonRoutingType with editor(invoke_object_function). If the world has shapes but no lanes then the graph has not been built, and the note says how to trigger it. Needs the ZoneGraph plugin. Returns data[], dataActorCount, laneCount, zoneShapeActorCount, plus found/laneIndex/distance/nearestPoint for nearest.", "query_zone_graph"),
    get_navmesh_details:    specBp("read", "Read RecastNavMesh generation params (cellSize, agentHeight, maxStepHeight, etc.) (#163).", "get_navmesh_details"),
    ...epicActions,
  },
  {
    ...epicSchema,
    // #1057: every key a spec'd handler declares, generated from its C++
    // registration. The tool exposes the physics and mass categories too, and
    // gameplay's own specs are spread last.
    ...physicsSpecSchema,
    ...massSpecSchema,
    ...specSchema,
  },
);
